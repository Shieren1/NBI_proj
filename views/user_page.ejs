<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link rel="stylesheet" href="/css/user_page.css">
</head>
<body>
    <header>
        <h1>User Dashboard</h1>
        <nav>
            <a href="/logout">Logout</a>
        </nav>
    </header>

    <main>
        <section class="welcome">
            <h2>Welcome, <%= user.firstname %> <%= user.lastname %>!</h2>
            <p>Your role: <strong><%= user.roles %></strong></p>
        </section>

        <section class="profile-info">
            <h2>Your Profile</h2>
            <ul>
                <li><strong>User ID:</strong> <%= user.user_id %></li>
                <li><strong>Email:</strong> <%= user.email %></li>
                <li><strong>Age:</strong> <%= user.age %></li>
                <li><strong>Gender:</strong> <%= user.gender %></li>
                <li><strong>Contact Number:</strong> <%= user.contact_num %></li>
                <li><strong>Location:</strong> <%= user.sitio %>, <%= user.barangay %>, <%= user.province %></li>
            </ul>
        </section>

        <section class="user-actions">
            <h2>Actions</h2>
            <ul>
                <li><a href="/order-history">View Order History</a></li>
                <li><a href="/settings">Account Settings</a></li>
                <li><a href="/profile">Profile</a></li>
                
            </ul>
        </section>
    </main>
</body>
</html>
